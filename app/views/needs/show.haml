=row :card do
  %h3= @need.title
  %p= @need.description
  =link_to new_need_donation_path(@need), class:"btn" do
    Donate
  =link_to 'Delete Need', @need, :method => :delete, :data => { :confirm => 'Are you sure?' }
