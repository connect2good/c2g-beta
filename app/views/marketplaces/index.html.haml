%h2 Display all marketplace transactions
%table 
  %tr
    %th ID
    %th Good ID
    %th Status
  - @marketplaces.each do |item|
    %tr
      / # %td== title #{item.title}
      %td= item.id
      / # %td== description #{item.description}
      %td= item.good_id
      %td= item.status
